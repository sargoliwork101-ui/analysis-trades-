# 🏗 معماری ماژولار نبض بازار

این سند «قرارداد» ساختار برنامه است. دلیل وجودش: تا نسخه‌ی ۱٫۸ منطقِ هر قابلیت
در چند فایل پراکنده بود (مثلاً مقایسه‌ی id منبع با رشته در ۱۲ جای مختلف و ساختنِ
متن «قیمت + واحد» در ۶ جای مختلف) — نتیجه‌اش همان باگ‌های «این قابلیت اینجا هست،
آنجا نیست» بود. از ۱٫۹ هر مفهوم **یک خانه‌ی واحد** دارد.

> ⚖️ اصل: **هر تغییری باید از کمترین تعداد جا شروع شود.** اگر برای افزودن یک
> قابلیت مجبوری در چند فایل همان شرط را تکرار کنی، جایش اینجا نیست — اول ماژول
> مرجعش را بساز.

---

## نقشه‌ی ماژول‌ها

```
com.pulse.market
│
├─ data/                     ← لایه‌ی داده — تنها دادِ برنامه با شبکه و تنظیمات
│   ├─ Model.kt              مدل‌ها: SourceDef / SymbolDef / Quote / WidgetConfig
│   ├─ MarketKind.kt    ★    تنها مرجع «هر منبع مال کدام بازار است»
│   ├─ SourceCatalog.kt      منابع آماده (تنها جا که id منابع «معنی» پیدا می‌کند)
│   ├─ Fetcher.kt            موتور گرفتن (JSON / HTML / TSE) — واحد را روی Quote می‌نشان‌د
│   ├─ QuoteRepo.kt          کش + تاریخچه + قانون stale (داده‌ی سالم هرگز پاک نمی‌شود)
│   ├─ ConfigStore.kt        تنظیمات هر ویجت + منابع/نمادهای دلخواه (DataStore)
│   ├─ TseModel.kt           سرویس بورس تهران (جستجو/قیمت/لایه‌های fallback)
│   ├─ WebSymbolSearch.kt    جستجوی آنلاین نماد (کریپتو/آمریکا) — با MarketKind
│   ├─ MarketStatus.kt       باز/بسته بودن بازارها — با MarketKind
│   ├─ Alert.kt / AlertEngine.kt   هشدار قیمت + نوتیف
│   └─ JsonPath.kt / Num.kt  مسیریابی JSON و پاک‌سازی اعداد فارسی
│
├─ ui/                       ← ظاهر
│   ├─ QuoteText.kt     ★    تنها جایی که متن «قیمت/واحد/تغییر/حجم» ساخته می‌شود
│   ├─ Format.kt             فرمت پایه‌ی اعداد (لایه‌ی زیر QuoteText)
│   ├─ SymbolSearchDialog.kt جستجوی نماد همه‌ی منابع
│   ├─ AddSourceDialog.kt / AddAlertDialog.kt
│   └─ settings/             صفحه‌ی تنظیمات (منو + بخش‌ها + قطعات مشترک)
│
├─ widget/                   ← ویجت
│   ├─ StockWidgetProvider.kt   زمان‌بندی/رفرش؛ تازگی و واحد را per-widget آماده می‌کند
│   └─ WidgetRenderer.kt        رندر RemoteViews — فقط نمایش، بدون منطق داده
│
├─ service/                  ← پس‌زمینه
│   ├─ LiveUpdateService.kt / LiveUpdateWorker.kt / BootReceiver.kt
│
└─ App.kt                    همگام‌سازی سرویس زنده با هر بالا آمدن پروسه
```

★ = ماژول مرجع تازه (از ۱٫۹)

---

## چهار قانون طلایی

### ۱) هیچ مقایسه‌ی رشته‌ایِ id منبع، بیرون از `SourceCatalog`/`MarketKind`
```kotlin
// ❌ قدیم — در ۱۲ جای برنامه پراکنده بود و باگ‌زاست:
if (src.id == "tse_tsetmc") { ... }

// ✅ درست:
if (src.marketKind == MarketKind.TSE) { ... }
// یا برای یک id تنها:
if (marketKindOf(symbol.sourceId) == MarketKind.TSE) { ... }
```
منبع جدید یا دلخواه که اضافه شود، همه‌ی قابلیت‌ها (وضعیت بازار، جستجو، فهرست
نمادها، هشدار) خودشان از `MarketKind` پیروی می‌کنند — هیچ `if` جدیدی لازم نیست.

### ۲) هیچ متن قیمتی بیرون از `QuoteText` ساخته نشود
```kotlin
// ❌ قدیم — واحد در بعضی جاها جا می‌افتاد:
"${Format.price(q.price)} ${q.unit}"

// ✅ درست:
QuoteText.priceWithUnit(q)          // «۶۴٬۲۱۰ تومان»
QuoteText.change(q)                 // «▲ ۲٫۳۱٪»
QuoteText.volume(q)                 // «۱۲٫۴ میلیون»
```
همین یک قانون باعث شد واحدِ نماد در ویجت، جستجو، هشدار، تستِ داده و نوتیف
همیشه یکسان باشد.

### ۳) ویجت فقط «نمایش» است؛ داده در `data/` آماده می‌شود
- `StockWidgetProvider.renderOne` داده‌ی هر ویجت را آماده می‌کند: پر کردن واحدِ
  خالی از تعریف منبع، تازگی per-widget، تاریخچه‌ی نمودار.
- `WidgetRenderer` فقط می‌کشد — نه شبکه، نه تصمیم داده.

### ۴) شبکه فقط از `QuoteRepo`/`Fetcher` — و داده‌ی سالم هرگز پاک نمی‌شود
- خطا = `stale` (چراغ قرمز) روی آخرین مقدار سالم؛ نه پاک‌کردن.
- کش فقط با داده‌ی سالم merge می‌شود.

---

## جریان داده (خلاصه)

```
WidgetConfig (هر ویجت مستقل)
   → StockWidgetProvider.refreshAll        چه کسی شبکه می‌خواهد؟ (زنده/بازه‌ی ساعتی)
   → QuoteRepo.refreshMany                 گرفتن + merge کش + تاریخچه
   → renderOne (per-widget)                واحدِ خالی ← تعریف منبع؛ تازگی ← داده‌ی خودش
   → WidgetRenderer + QuoteText            متن‌ها از یک چرخه
```

## چک‌لیست تغییرات (برای آینده)

- [ ] قابلیت تازه؟ اول بپرس: مال کدام `MarketKind` است یا باید ماژول مرجع تازه باشد؟
- [ ] متن نمایشی تازه؟ فقط از طریق `QuoteText` (اگر شکلی جدید لازم است، متدش را به آن اضافه کن)
- [ ] تنظیم تازه؟ روی `WidgetConfig` — هر ویجت مستقل
- [ ] منبع تازه؟ فقط `SourceCatalog` (یا منبع دلخواه کاربر) — بقیه‌ی برنامه خودش پیدایش می‌کند
