# قوانین اجباری مخزن

## نسخه‌گذاری

- برای **هر تغییر** در مخزن، حتی تغییر کوچک، رفع اشکال یا مستندات، باید نسخه‌ی برنامه افزایش پیدا کند.
- در `app/build.gradle.kts` مقدار `versionCode` باید حداقل یک واحد زیاد شود و `versionName` نیز به نسخه‌ی بعدی تغییر کند.
- هیچ مجموعه‌تغییری بدون نسخه‌ی تازه commit یا push نشود.
- نسخه‌ی تازه در تاریخچه‌ی نسخه‌های `README_fa.md` و `README.md` ثبت شود.
- build، تست‌های واحد و Android Lint نسخه‌ی تازه باید پیش از اعلام پایان کار موفق باشند.
