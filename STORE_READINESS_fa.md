# آمادگی انتشار «نبض بازار» در اپ‌استور

تاریخ بررسی: ۴ مهر ۱۴۰۵ / ۲۶ سپتامبر ۲۰۲۶

## جمع‌بندی

نسخه‌ی فعلی برای انتشار مستقیم APK از GitHub قابل ساخت و آزمایش است، اما **در وضعیت فعلی آماده‌ی انتشار در Google Play نیست**. مهم‌ترین موانع، Target API قدیمی، سرویس زنده‌ی دائمی، کلید امضای عمومی، خروجی Debug به‌جای AAB انتشار و به‌روزرسانی از GitHub هستند.

این سند Google Play را به‌عنوان سخت‌گیرانه‌ترین مبنا در نظر می‌گیرد. برای بازار، مایکت یا فروشگاه‌های دیگر نیز امضای خصوصی، نسخه‌ی Release، سیاست حریم خصوصی، اطلاعات فروشگاه و آزمون واقعی لازم‌اند؛ جزئیات فرم‌ها و محدودیت به‌روزرسانی باید با سیاست همان فروشگاه تطبیق داده شود.

---

## ۱) موانع فنی انتشار در Google Play

### ۱.۱) Target API — مسدودکننده

پروژه اکنون `compileSdk = 34` و `targetSdk = 34` دارد. از ۳۱ اوت ۲۰۲۶، برنامه‌ها و به‌روزرسانی‌های تازه در Google Play باید Android 16 / API 36 را هدف بگیرند.

کار لازم:

- ارتقای `compileSdk` و `targetSdk` به 36؛
- ارتقای Android Gradle Plugin از 8.5.2 به نسخه‌ای سازگار با API 36 (حداقل رسمی: 8.9.1) و Gradle متناظر؛
- اجرای تست روی Android 8، 13، 14، 15 و 16؛
- بررسی edge-to-edge، Predictive Back، اعلان‌ها و محدودیت‌های پس‌زمینه.

منابع رسمی:

- <https://support.google.com/googleplay/android-developer/answer/11926878>
- <https://developer.android.com/about/versions/16/setup-sdk>
- <https://developer.android.com/build/releases/about-agp>

### ۱.۲) سرویس زنده‌ی دائمی — مسدودکننده

`LiveUpdateService` از نوع `dataSync` است و برای تازه‌سازی ۵ تا ۱۲۰ ثانیه‌ای می‌تواند دائماً روشن بماند. برای برنامه‌های هدف‌گیرنده‌ی Android 15 به بالا:

- مجموع اجرای dataSync ForegroundService در پس‌زمینه به ۶ ساعت در هر ۲۴ ساعت محدود است؛
- پس از timeout باید سرویس ظرف چند ثانیه متوقف شود، وگرنه برنامه crash می‌کند؛
- شروع dataSync FGS از `BOOT_COMPLETED` ممنوع است؛
- Google Play استفاده از FGS را فقط برای کارِ ضروری، آغازشده توسط کاربر و قابل‌درک برای کاربر می‌پذیرد و برای آن اظهارنامه و ویدئوی نمایش قابلیت می‌خواهد.

در این ممیزی شروع سرویس از بوت/ساخت پس‌زمینه‌ی پروسه حذف و به WorkManager منتقل شد، اما معماری «زنده برای مدت نامحدود» همچنان قبل از Target 36 باید تصمیم‌گیری شود.

راه پیشنهادی برای نسخه‌ی فروشگاهی:

1. حالت استاندارد: WorkManager با حداقل فاصله‌ی ۱۵ دقیقه + رفرش دستی؛
2. حالت زنده: فقط با اقدام صریح کاربر، برای یک جلسه‌ی محدود و با دکمه‌ی توقف؛
3. پیاده‌سازی `Service.onTimeout` پس از ارتقای compileSdk؛
4. برای هشدار تقریباً لحظه‌ای و قابل‌اعتماد، انتقال پایش به backend و ارسال FCM؛ چون Android اجرای دائمی ۱۵ ثانیه‌ای را تضمین نمی‌کند.

منابع رسمی:

- <https://developer.android.com/develop/background-work/services/fgs/timeout>
- <https://developer.android.com/about/versions/15/behavior-changes-15>
- <https://support.google.com/googleplay/android-developer/answer/13392821>
- <https://support.google.com/googleplay/android-developer/answer/16559646>

### ۱.۳) امضای انتشار — مسدودکننده

فایل `app/ci-debug.keystore` با رمز عمومی `android` داخل مخزن است و build نوع `release` نیز همین کلید را مصرف می‌کند. این کلید برای انتشار قابل اعتماد نیست؛ هرکس به مخزن دسترسی دارد می‌تواند APK هم‌امضا تولید کند.

کار لازم:

- ساخت کلید خصوصی انتشار/Upload Key جدید؛
- نگهداری کلید و رمزها فقط در Secretهای CI یا سامانه‌ی امن آفلاین؛
- فعال‌کردن Play App Signing؛
- ساخت AAB امضاشده با Upload Key؛
- نگهداری نسخه‌ی پشتیبان امن و مستندات بازیابی کلید.

نکته: APKهای فعلی که با کلید عمومی نصب شده‌اند، نسخه‌ی دارای امضای جدید را به‌صورت update نمی‌پذیرند. پیش از انتشار باید درباره‌ی نصب مجدد کاربران فعلی یا package name تازه تصمیم گرفته شود.

منبع رسمی: <https://developer.android.com/studio/publish/app-signing>

### ۱.۴) خروجی انتشار — مسدودکننده

CI فعلی فقط `assembleDebug` می‌سازد. برای برنامه‌ی تازه‌ی Google Play باید Android App Bundle ارائه شود.

کار لازم:

- تعریف signing انتشار از environment/secret، نه فایل مخزن؛
- اجرای `bundleRelease`؛
- اجرای `test`, `lintRelease` و در صورت فعال‌سازی کوچک‌سازی، آزمون build minified؛
- بارگذاری `.aab` در Internal Testing؛
- افزایش `versionCode` در هر انتشار.

منابع رسمی:

- <https://support.google.com/googleplay/android-developer/answer/9859152>
- <https://support.google.com/googleplay/android-developer/answer/9859348>

### ۱.۵) به‌روزرسانی بیرون از فروشگاه — مسدودکننده برای Play

صفحه‌ی درباره، APK نسخه‌ی جدید را از GitHub Releases پیشنهاد می‌کند. برنامه‌ای که از Google Play توزیع می‌شود نباید خودش را با روشی خارج از سازوکار Google Play به‌روزرسانی کند.

راه‌حل:

- ساخت flavor جداگانه‌ی `play` و حذف کامل دکمه/کد دانلود APK از آن؛
- استفاده از Play In-App Updates یا واگذاری کامل update به فروشگاه؛
- در flavor مستقل/GitHub می‌توان updater فعلی را نگه داشت؛
- برای بازار یا مایکت، کاربر به صفحه‌ی همان فروشگاه هدایت شود و سیاست همان فروشگاه رعایت شود.

منبع رسمی: <https://support.google.com/googleplay/android-developer/answer/12085295>

### ۱.۶) Cleartext HTTP — ریسک امنیتی بالا

`network_security_config.xml` برای همه‌ی دامنه‌ها `cleartextTrafficPermitted=true` دارد. منابع داخلی فعلی همگی HTTPS هستند و این مجوز فقط برای منبع دلخواه باقی مانده است. در HTTP، قیمت، هدر و API Key احتمالی قابل مشاهده یا تغییر در مسیر شبکه‌اند.

پیشنهاد:

- flavor فروشگاهی فقط HTTPS را بپذیرد و cleartext را غیرفعال کند؛
- اگر HTTP برای نسخه‌ی مستقل ضروری است، هشدار برجسته و بدون هدر حساس باقی بماند؛
- هرگز Authorization/API Key روی HTTP ارسال نشود.

---

## ۲) حریم خصوصی و سیاست‌های فروشگاه

### ۲.۱) Privacy Policy — مسدودکننده

اکنون لینک یا متن سیاست حریم خصوصی داخل برنامه وجود ندارد و در وب‌سایت معرفی‌شده نیز صفحه‌ی مشخصی برای «نبض بازار» پیدا نشد.

سیاست باید روی URL عمومی، پایدار، غیر PDF و بدون محدودیت جغرافیایی منتشر و از داخل برنامه نیز قابل دسترسی باشد. حداقل موارد:

- نام برنامه و هویت/راه تماس توسعه‌دهنده؛
- اینکه حساب کاربری، تبلیغات و analytics وجود ندارد؛
- تنظیمات، هشدارها، کش قیمت و تاریخچه روی دستگاه ذخیره می‌شوند؛
- درخواست‌های شبکه مستقیماً به CoinGecko، TSETMC، TGJU، TradingView، GitHub و منابع دلخواه کاربر می‌روند و آن سرویس‌ها IP/اطلاعات فنی درخواست را دریافت می‌کنند؛
- فایل بکاپ رمزگذاری نشده و ممکن است هدر/API Key منبع دلخواه را داشته باشد؛
- سیاست نگهداری و حذف داده: حذف برنامه/پاک‌کردن داده و حذف فایل بکاپ توسط کاربر؛
- رویه‌ی پاسخ‌گویی به درخواست‌های حریم خصوصی.

فرم Data safety نیز باید دقیقاً با همین رفتار هماهنگ باشد. برنامه حساب نمی‌سازد، بنابراین الزام حذف حساب ندارد، ولی پرسش‌های حذف داده باید پاسخ داده شوند.

منابع رسمی:

- <https://support.google.com/googleplay/android-developer/answer/10144311>
- <https://support.google.com/googleplay/android-developer/answer/10787469>

### ۲.۲) مالی و رمزارز

برنامه معامله، کیف پول یا انتقال وجه انجام نمی‌دهد، ولی قیمت سهام/رمزارز، هشدار و امتیاز پامپ نمایش می‌دهد. لازم است:

- فرم Financial features در Play Console تکمیل شود؛ حتی برنامه‌های بدون قابلیت مالی باید این فرم را پاسخ دهند؛
- قابلیت واقعی با گزینه‌ی درست (احتمالاً اطلاعات/پشتیبانی مالی یا Other، نه «Stock trading») اعلام شود؛
- در توضیحات فروشگاه و داخل برنامه نوشته شود: «اطلاعات با تأخیر یا خطا ممکن است؛ توصیه‌ی سرمایه‌گذاری یا سیگنال خرید/فروش نیست»؛
- هیچ ادعای سود، دقت تضمینی یا «لحظه‌ای تضمین‌شده» استفاده نشود؛
- اگر Play این محصول را خدمت مالی طبقه‌بندی کند، حساب Organization و احراز هویت/D-U-N-S ممکن است لازم شود.

منابع رسمی:

- <https://support.google.com/googleplay/android-developer/answer/13849271>
- <https://support.google.com/googleplay/android-developer/answer/13634885>

### ۲.۳) مجوز و شرایط منابع داده

پیش از استفاده‌ی تجاری باید شرایط استفاده، attribution، rate limit و اجازه‌ی بازنشر داده‌ی CoinGecko، TSETMC، TGJU و TradingView بررسی و مستند شود. وجود endpoint عمومی به‌تنهایی مجوز بازنشر در محصول تجاری نیست. نام/نشان تجاری منابع نیز نباید وابستگی رسمی را القا کند.

---

## ۳) اقلام لازم برای صفحه‌ی فروشگاه

- نام برنامه (حداکثر ۳۰ نویسه در Play)؛
- توضیح کوتاه (حداکثر ۸۰ نویسه) و توضیح کامل (حداکثر ۴۰۰۰ نویسه)؛
- آیکن Play با ابعاد ۵۱۲×۵۱۲ PNG؛
- Feature Graphic با ابعاد ۱۰۲۴×۵۰۰؛
- حداقل ۲ اسکرین‌شات واقعی؛ پیشنهاد: حداقل ۴ تصویر ۱۰۸۰×۱۹۲۰؛
- دسته‌بندی مناسب (Finance یا Tools پس از تصمیم سیاستی)؛
- ایمیل پشتیبانی معتبر، وب‌سایت و سیاست حریم خصوصی؛
- پرسش‌نامه‌ی Content Rating؛
- Target audience؛ بهتر است برنامه برای کودکان معرفی نشود؛
- فرم‌های Data safety، Financial features، Ads (اعلام «بدون تبلیغ» در صورت حفظ وضعیت فعلی) و Foreground service؛
- توضیح روشن دسترسی اعلان و سرویس پس‌زمینه؛
- Release notes فارسی و انگلیسی در صورت انتشار بین‌المللی.

منبع رسمی دارایی‌ها: <https://support.google.com/googleplay/android-developer/answer/9866151>

---

## ۴) آزمون پیش از انتشار

حداقل ماتریس پیشنهادی:

- Android 8 / API 26: نصب، ویجت، شبکه و فونت؛
- Android 13 / API 33: رد و قبول مجوز اعلان؛
- Android 14 / API 34: شروع ForegroundService و اعلان دائمی؛
- Android 15 / API 35: timeout سرویس، بوت و محدودیت پس‌زمینه؛
- Android 16 / API 36: edge-to-edge، Back، ویجت و محدودیت‌های جدید؛
- حالت بدون اینترنت، DNS خراب، HTTP 429، پاسخ ناقص/بزرگ و timeout؛
- reboot، force-stop، حذف/افزودن چند ویجت و آپدیت نسخه؛
- فونت بزرگ، RTL، باتری‌سیور و Data Saver؛
- Pre-launch report و Android Vitals (Crash/ANR) پس از Internal Testing.

برای حساب شخصی Play ساخته‌شده بعد از ۱۳ نوامبر ۲۰۲۳، پیش از Production معمولاً Closed Test با حداقل ۱۲ تسترِ opt-in به‌مدت ۱۴ روز لازم است:
<https://support.google.com/googleplay/android-developer/answer/14151465>

---

## ۵) ترتیب اجرای پیشنهادی

1. تصمیم درباره‌ی فروشگاه هدف و package name؛
2. ساخت کلید خصوصی انتشار و flavorهای `play` / `standalone`؛
3. بازطراحی حالت زنده برای محدودیت FGS؛
4. ارتقای پروژه به compile/target 36؛
5. حذف updater بیرونی و HTTP از flavor فروشگاهی؛
6. ساخت Privacy Policy، سلب مسئولیت و فرم‌های Play Console؛
7. بررسی حقوق و شرایط منابع داده؛
8. تولید AAB انتشار و اجرای تست‌های دستگاهی/Pre-launch؛
9. تهیه‌ی دارایی‌های فروشگاه؛
10. Internal → Closed → Production با rollout مرحله‌ای.
