# 🔍 ممیزی امنیت و مهندسی — نبض بازار (تا نسخه‌ی ۱٫۱۸)

این سند نتیجه‌ی خواندن کامل کد برنامه (بیش از ۷ هزار خط کاتلین) و اصلاحاتی است
که در همین نسخه اعمال شد. هر مورد سه بخش دارد: **یافته**، **چرا مهم است** و **چه شد**.

> وضعیت: همه‌ی موارد «اصلاح‌شده» در همین نسخه پیاده شده‌اند؛ موارد «باقی‌مانده» به
> تصمیم شما نیاز دارند و در پایان سند آمده‌اند.

---

## ۱) امنیت

### ۱.۱) پیام‌های ویجت از بیرون قابل جعل بودند — 🔴 اصلاح شد
**یافته:** رسیورهای ویجت (`StockWidgetProvider` و دو زیرکلاسش) در مانیفست
`exported="true"` بودند و اکشن‌های سفارشی `ACTION_REFRESH` و
`ACTION_TOGGLE_LIVE` را هم می‌پذیرفتند. برخلاف توضیح قدیمی این فایل، AppWidgetProvider
برای دریافت پیام سیستم لازم نیست exported باشد. هر برنامه‌ی دیگری روی گوشی می‌توانست
با یک `sendBroadcast`، رفرش اجباری یا تغییر حالت زنده را تحمیل کند.

**چه شد:** هر سه رسیور مطابق راهنمای Android به `exported="false"` تغییر کردند.
`InternalGuard` هم به‌عنوان دفاع دوم باقی ماند: PendingIntentهای تغییرناپذیر با توکن
خصوصی امضا می‌شوند و اکشن بدون توکن رد می‌شود. ساخت توکن نیز همگام شد تا در رندرهای
هم‌زمان دو توکن متفاوت ساخته نشود.

### ۱.۲) انتخاب فایل نصبی در آپدیت‌کننده سهل‌گیرانه بود — 🟠 اصلاح شد
**یافته:** `AppUpdater` از assetهای ریلیز، «اولین فایلی که پسوندش apk بود» را برمی‌داشت
و همان را برای دانلود به کاربر می‌داد.

**چه شد:** فقط آدرسی پذیرفته می‌شود که با
`https://github.com/sargoliwork101-ui/analysis-trades-/releases/download/` شروع شود،
نام و آخرین بخش URL دقیقاً الگوی `PulseMarket-vN.N.apk` باشد. fallback باقی‌مانده‌ی
«هر APK داخل همان ریلیز» حذف شد و SHA-256 نیز فقط اگر دقیقاً ۶۴ رقم هگز باشد پذیرفته
و در صفحه‌ی «درباره» نشان داده می‌شود.

### ۱.۳) پاسخ‌های HTTP سقف حجم نداشتند — 🟠 اصلاح شد
**یافته:** همه‌ی ماژول‌های شبکه `resp.body?.string()` را بی‌سقف می‌خواندند. یک منبع
دلخواهِ اشتباه یا یک سرور خراب/مخرب می‌توانست پاسخ چند صد مگابایتی بدهد و برنامه را
با کمبود حافظه بکشد (DoS محلی).

**چه شد:** ماژول تازه‌ی `Http` — یک کلاینت مشترک با سقف حجم (JSON و HTML: ۸ مگابایت؛
عدد عمداً بزرگ است چون پاسخ TGJU خودش چند صد کیلوبایت است) و پیام خطای خوانا.
پاسخ غول‌آسا قبل از خوانده‌شدن کامل، رد می‌شود.

### ۱.۴) اعتبارسنجی آدرس ورودی — 🟡 اصلاح شد
**یافته:** آدرس منبع دلخواه بدون بررسی به OkHttp داده می‌شد؛ هر اسکیمی
(`file:`، `content:`، `intent:` …) قابل تزریق بود.

**چه شد:** در `Http` فقط `http://` و `https://` پذیرفته می‌شود؛ بقیه با پیام
«آدرس نامعتبر» رد می‌شوند. (فایل بکاپِ دست‌کاری‌شده هم دیگر نمی‌تواند منبع عجیب جا کند.)

### ۱.۵) پاک‌سازی منابع دلخواه ورودی از بکاپ — 🟡 اصلاح شد
**یافته:** `importAll` هر `SourceDef` داخل فایل بکاپ را می‌نوشت؛ حتی با
`builtIn = true` (منبعی که در UI دکمه‌ی حذف ندارد) یا با id یکی از منابع آماده.

**چه شد:** پیش از ذخیره و بازیابی، منابع و تنظیمات پاک‌سازی می‌شوند: `builtIn=false`،
حذف id تکراری داخلی، پذیرش فقط HTTP(S)، حذف NaN/Infinity، محدودکردن طول/تعداد
نمادها، هدرها، هشدارها و مقادیر زمانی. فایل ورودی نیز سقف ۲ مگابایت دارد و خواندن/
نوشتن آن از ترد UI خارج شد؛ بنابراین بکاپ دست‌کاری‌شده باعث OOM یا تنظیم نامعتبر نمی‌شود.

### ۱.۶) هشدار آدرس بدون رمزنگاری — 🟡 اصلاح شد
**یافته:** منابع دلخواه می‌توانند `http://` باشند (چون بعضی سایت‌های داخلی هنوز http
هستند) و در این حالت هر کسی در مسیر شبکه می‌تواند عدد قیمت را عوض کند.

**چه شد:** در پنجره‌ی «منبع دلخواه»، اگر آدرس با `http://` شروع شود، هشدار واضح
«رمزنگاری‌نشده — اگر سایت https دارد حتماً https بگذار» نشان داده می‌شود.
(سیاست cleartext در `network_security_config.xml` عمداً دست‌نخورده ماند تا منابع
دلخواه داخلی کار کنند.)

### ۱.۷) موارد بررسی‌شده و سالم
- همه‌ی `PendingIntent` ها `FLAG_IMMUTABLE` دارند (بدون آن، Intent Redirect ممکن بود).
- هیچ کلید/توکن/رمز ثابتی داخل کد یا مخزن نیست (به‌جز کلید امضای پایین).
- سرویس پیش‌زمینه `exported="false"` است؛ اعلان‌ها کانال و شناسه‌ی ثابت دارند.
- تنظیمات فقط در `DataStore`/`SharedPreferences` خصوصی برنامه ذخیره می‌شود و
  `allowBackup=false` است تا هدر احتمالی منبع دلخواه وارد بکاپ خودکار ابری نشود؛
  بکاپ دستی داخل برنامه همچنان کار می‌کند.
- جستجوی TSETMC ورودی کاربر را `URLEncoder` می‌کند (بدون تزریق مسیر).

### ۱.۸) موارد باقی‌مانده (نیاز به تصمیم شما) — 🟠
1. **کلید امضای APK در مخزن عمومی است** (`app/ci-debug.keystore`، رمز `android`).
   یعنی هر کسی می‌تواند APK هم‌امضا بسازد و اندروید آن را «آپدیت» نسخه‌ی نصب‌شده
   بپذیرد. کاری که در همین نسخه برای کم‌کردن ریسک انجام شد: آپدیت درون‌برنامه‌ای فقط
   از ریلیزهای همین مخزن می‌آید + اثر انگشت فایل به کاربر نشان داده می‌شود.
   راه‌حل ریشه‌ای (پیشنهاد): ساخت یک کلید خصوصی تازه، نگه‌داشتنش در
   **GitHub Secrets** و حذف فایل کلید از مخزن + خواندنش در CI از همان Secret.
   توجه: پس از این کار، APK تازه با امضای جدید روی نصب قدیمی **نمی‌نشیند** و یک بار
   حذف/نصب مجدد لازم است.
2. **گزارش خطا در برنامه ندارد**: خطاها در `Quote.error` نگه داشته می‌شوند و روی UI
   دیده می‌شوند؛ لاگ متمرکزی وجود ندارد (برای حریم خصوصی خوب است، برای دیباگ سخت‌تر).

---

## ۲) مهندسی و کارایی

### ۲.۱) ذخیره‌ی تنظیمات می‌توانست گم شود — 🔴 اصلاح شد
**یافته:** `ConfigStore.saveDebounced` فقط یک `Job` سراسری داشت؛ هر فراخوانی جدید
`cancel()` می‌کرد. اگر دو ویجت در فاصله‌ی کمتر از ۲۰۰ میلی‌ثانیه ذخیره می‌کردند
(یا الگو + یک ویجت)، ذخیره‌ی اولی **گم می‌شد**.

**چه شد:** نقشه‌ی `ConcurrentHashMap<Int, Job>` — نوبت هر ویجت جدا. Job پیش از شروع
به‌صورت `LAZY` در نقشه ثبت و حذفش شرطی می‌شود. ذخیره‌ی نهایی دکمه نیز حالا واقعاً
پیش از بستن Activity تمام می‌شود و بازیابی/حذف ویجت، ذخیره‌ی معوق قدیمی را لغو می‌کند.

### ۲.۲) کارایی کش قیمت و تاریخچه — 🟠 اصلاح شد
**یافته:** در هر رفرش، JSON کش و JSON تاریخچه چند بار از دیسک خوانده و دو بار جداگانه
نوشته می‌شد (روی نمادهای زیاد و ۲۴۰ نقطه تاریخچه، رشته‌ی چند صد کیلوبایتی).

**چه شد:** `QuoteRepo` حالا کش و تاریخچه را در حافظه نگه می‌دارد و فقط وقتی داده‌ی
تازه آمده باشد یک **تراکنش** واحد می‌نویسد (قیمت‌ها + تاریخچه + زمان با هم).

### ۲.۳) چهار کلاینت شبکه‌ی جداگانه — 🟠 اصلاح شد
`Fetcher`، `TseService`، `WebSymbolSearch` و `AppUpdater` هر کدام `OkHttpClient` خودشان
را با تایم‌اوت‌های متفاوت می‌ساختند (چند استخر اتصال و ترد اضافه). حالا همه از
`Http.client` مشترک استفاده می‌کنند.

### ۲.۴) بدون سقف درخواست هم‌زمان — 🟡 اصلاح شد
با چند ویجت و چند منبع، تعداد درخواست‌های موازی بی‌مرز می‌شد. حالا `Fetcher` با
`Semaphore(4)` حداکثر ۴ درخواست هم‌زمان می‌فرستد (شامل درخواست‌های batch و تست داده).
رفرش کامل و تراکنش کش/تاریخچه نیز با Mutex سری شده تا رسیور، سرویس و Worker درخواست
تکراری هم‌زمان یا merge ازدست‌رفته نسازند.

### ۲.۵) نوشتن‌های اضافه در موتور هشدار — 🟡 اصلاح شد
`AlertEngine` برای هر قانون تا دو بار روی دیسک می‌نوشت (۱۰ هشدار = ۲۰ نوشتن در هر
رفرش). حالا وضعیت همه‌ی قانون‌ها در یک `SharedPreferences.Editor` جمع و یک‌بار
`apply` می‌شود.

### ۲.۶) تمیزی کد — 🟡 اصلاح شد
- `MainActivity.finishConfigure(cfg)` پارامتر بی‌استفاده داشت و `WidgetConfig` بی‌مصرف
  ایمپورت شده بود (ذخیره‌ی واقعی در `SettingsScreen` با `NonCancellable` انجام می‌شد).
- ایمپورت‌ها و کلاینت‌های مرده در `TseModel`/`WebSymbolSearch` پاک شدند.

### ۲.۷) هیچ تستی وجود نداشت — 🟠 اصلاح شد
**چه شد:** پوشه‌ی `app/src/test/` با تست‌های واحد JVM (بدون شبیه‌ساز):
`JsonPath` (مسیرهای تودرتو/آرایه/چندمسیری/عددِ رشته‌ای TGJU)، `Num` (ارقام و ممیز
فارسی، جداکننده‌ها، رد عدد نامتناهی)، `AppUpdater` (نسخه و انتخاب امن APK)،
`PumpScanner` (امتیاز، سقف حجم، سطح ریسک)، `Fetcher` (واحد/ضریب و فرمول درصد) و
`AlertLogic` (عبور درصدی، روز خالی و بازه‌ی شب‌گذر).
مرحله‌های `./gradlew test` و `lintDebug` هم به CI اضافه شدند تا خرابی منطق یا خطای Android Lint، ران را قرمز کند.

### ۲.۸) موارد جزئی که عمداً تغییر نکردند
- کانال اعلان‌ها و ظاهر برنامه دست‌نخورده ماندند.
- `LiveUpdateService` با `START_STICKY`: درست است، چون سرویس خودش در نبود ویجت زنده
  `stopSelf` می‌کند (در ممیزی نسخه‌ی ۱٫۱۳ اضافه شده بود).
- هیچ rate-limit سمت کلاینت برای CoinGecko گذاشته نشد؛ به‌جایش کش ۲ دقیقه‌ایِ اسکن
  پامپ‌ها و درخواست گروهیِ منبع کریپتو. اگر باز هم ۴۲۹ بدهد، پیام خطای خوانا به کاربر
  نشان داده می‌شود و نتیجه‌ی قبلی روی صفحه می‌ماند.

---

## ۳) باگ‌های کارکردی که در ممیزی پیدا و رفع شد

| # | باگ | اثر | وضعیت |
|---|-----|-----|-------|
| ۱ | منبع TGJU برای **همه‌ی** نمادها ضریب ۰٫۱ و واحد «تومان» داشت | «انس طلای جهانی» ۱۰ برابر کوچک‌تر و با واحد تومان نشان داده می‌شد | ✅ حالا هر نماد واحد/ضریب خودش را دارد |
| ۲ | ذخیره‌ی debounce سراسری | گم‌شدن تنظیمات یکی از دو ویجت (بند ۲٫۱) | ✅ |
| ۳ | بخش «انس جهانی» از فهرست نمادهای آماده وجود نداشت | کاربر برای طلای جهانی کد `ons` را دستی تایپ می‌کرد و ضریبش هم غلط بود | ✅ نماد و منبع آماده اضافه شد |
| ۴ | هشدار درصدی در حالت «فقط عبور» قیمت قبلی را به‌جای درصد قبلی مقایسه می‌کرد | بعد از ارزیابی اول، عبورهای بعدی دیده نمی‌شد | ✅ متریک هم‌نوع هر شرط ذخیره می‌شود |
| ۵ | انتخاب هیچ روزی در هشدار مثل انتخاب همه‌ی روزها بود | اعلان در روزهایی که کاربر خاموش کرده بود | ✅ مجموعه‌ی خالی یعنی هیچ روز |
| ۶ | فرمول `ABSOLUTE/PREV_CLOSE` قیمت خام را دوباره بر ضریب تقسیم می‌کرد | درصد منبع دلخواه با ضریب غیر ۱ غلط بود | ✅ محاسبه در واحد خام یکسان |
| ۷ | ممیز فارسی `٫` حذف می‌شد | `۴٬۳۱۰٫۹۴` به‌صورت `۴۳۱۰۹۴` خوانده می‌شد | ✅ نرمال‌سازی ممیز و علامت منفی یونیکد |
| ۸ | دکمه‌ی ذخیره صفحه را قبل از شروع coroutine می‌بست | ذخیره‌ی نهایی گاهی لغو می‌شد | ✅ بستن فقط بعد از پایان ذخیره |
| ۹ | هشدار می‌توانست از Quote کش‌شده یا stale ساخته شود | اعلان بر اساس قیمت قدیمی | ✅ فقط داده‌ی سالم همان درخواست شبکه |

---

## ۴) قابلیت‌های تازه‌ی همین نسخه

1. **طلای جهانی** — در منبع «طلا و ارز — TGJU» نمادهای «انس طلا (جهانی)»،
   «انس نقره (جهانی)» و «طلای توکنیزه XAU» با واحد **$** و ضریب ۱ اضافه شدند؛
   «پوند انگلیس» هم که کلیدش در TGJU بود به فهرست آمد.
2. **منبع آماده‌ی «بازارهای جهانی — TradingView»** — طلا (OANDA:XAUUSD)، نقره،
   نفت WTI و برنت، شاخص دلار و بیت‌کوین/اتریوم. واحد هر نماد جدا تعریف شده
   (دلار/امتیاز). ساعت بازار «جهانی» (یکشنبه ۱۸ نیویورک تا جمعه ۱۷) هم به وضعیت
   بازارها اضافه شد.
3. **بخش «پامپ‌های کریپتو»** — معرفی و آموزش پامپ/پامپ-اند-دامپ + اسکن زنده‌ی
   کوین‌های برتر CoinGecko با امتیاز ترکیبی (رشد ۲۴ساعت + ۲×رشد ۱ساعت +
   ۵۰×نسبت حجم به ارزش بازار)، برچسب ریسک، قواعد احتیاط و دکمه‌ی «افزودن به ویجت».
   با یک کلید می‌توانید کل بخش را از منوی تنظیمات مخفی کنید.

---

## ۵) چک‌لیست تست پیشنهادی روی گوشی (پس از نصب ۱٫۱۴)

- [ ] ویجت بسازید، منبع «بازارهای جهانی — TradingView» را روشن کنید و نماد
      «طلا — انس جهانی» را انتخاب کنید → قیمت دلاری با واحد `$` و ساعت به‌روزرسانی.
- [ ] همان کار را با منبع TGJU و نماد «انس طلا (جهانی)» انجام دهید
      (برای اینترنت ایران این گزینه مطمئن‌تر است).
- [ ] بخش «پامپ‌های کریپتو» → «اسکن تازه» → فهرست کوین‌ها + افزودن یکی به ویجت.
- [ ] دو ویجت مختلف را پشت‌سرهم تغییر دهید و اپ را ببندید → هر دو تنظیمات بمانند.
- [ ] با خاموش‌کردن نت، ویجت باید آخرین قیمت‌ها را نگه دارد و LEDها قرمز شوند.
- [ ] «بررسی آپدیت» در بخش درباره → نسخه‌ی جدید و اثر انگشت فایل نمایش داده شود.

---

## ۶) ممیزی دوم عملکرد و آمادگی فروشگاه (سپتامبر ۲۰۲۶)

در بازخوانی کامل دوم، موارد زیر نیز پیدا و اصلاح شدند:

| یافته | اثر قبلی | اصلاح |
|---|---|---|
| `App` و `BootReceiver` رفرش را بدون رعایت حالت دستی/بازه اجرا می‌کردند | مصرف شبکه خارج از ساعت انتخابی و حتی برای ویجت دستی | شروع شبکه از ساخت پس‌زمینه‌ی پروسه حذف شد؛ بوت یک WorkManager متصل به شبکه صف می‌کند و تنظیمات را رعایت می‌کند |
| بوت می‌توانست `dataSync` ForegroundService را شروع کند | با Target 35+ ممنوع و مستعد `ForegroundServiceStartNotAllowedException` | شروع FGS از بوت/پس‌زمینه حذف شد؛ فقط پس از Activity یا اقدام مستقیم کاربر شروع می‌شود |
| Back در زیرصفحه‌ی تنظیمات، کل Activity/جریان افزودن ویجت را می‌بست | خروج ناخواسته به‌جای برگشت به منو | `BackHandler` زیرصفحه را اول به منوی تنظیمات برمی‌گرداند |
| خاموش‌کردن «ساعت بالای ویجت» در حضور وضعیت بازار بی‌اثر بود | ساعت همچنان نمایش داده می‌شد | متن ساعت و وضعیت بازار مستقل ساخته می‌شوند |
| ورودی فارسیِ حد هشدار پذیرفته نمی‌شد و `NaN` ممکن بود ظاهراً ذخیره شود | دکمه‌ی ذخیره برای ارقام فارسی غیرفعال یا قانون پس از reload حذف می‌شد | پارس با `Num`، رد non-finite و نرمال‌سازی درصد با قدرمطلق |
| preset «ساعات بورس تهران» پنجشنبه را هم فعال می‌کرد | هشدار خارج از روز معاملاتی تعریف‌شده‌ی برنامه | روزها به شنبه تا چهارشنبه اصلاح شدند |
| پارامتر `persian` در متن زمان‌بندی هشدار نادیده گرفته می‌شد | ساعت هشدار با ارقام لاتین می‌ماند | تبدیل واقعی ارقام اعمال شد |
| تغییر آستانه‌ی اسکن پامپ تا اسکن شبکه‌ی بعدی اثری نداشت | چیپ ۳/۵/۸٪ انتخاب می‌شد ولی فهرست قدیمی بود | آستانه فوراً روی داده‌ی کش‌شده فیلتر می‌شود |
| ضریب تبدیل قیمت روی حجم هم اعمال می‌شد | حجم منبع دارای scale اشتباه نمایش داده می‌شد | scale فقط روی قیمت/سری قیمت اعمال می‌شود |
| منبع فعالِ بدون نماد، نمادهای پنهان پیش‌فرض نشان می‌داد | تنظیمات و خروجی ویجت با هم ناسازگار بودند | پیش‌فرض فقط برای پیکربندی کاملاً خالی استفاده می‌شود |
| ذخیره‌ی منبع/نماد سفارشی به‌صورت fire-and-forget بود | امکان race با ذخیره‌ی تنظیمات ویجت یا خروج سریع | فراخوانی‌های UI منتظر پایان DataStore می‌مانند |
| خرابی فایل Preferences recovery نداشت | امکان ازکارافتادن دائمی جریان تنظیمات/ویجت | `ReplaceFileCorruptionHandler` به تنظیمات خالی سالم برمی‌گردد |
| export می‌توانست آخرین debounce را جا بیندازد | بکاپ یک تغییر عقب‌تر بود | پیش از export همه‌ی ذخیره‌های معوق join می‌شوند |
| متن UI ادعا می‌کرد بکاپ هیچ رمز/کلیدی ندارد | برای منبع importشده با هدر/API Key نادرست بود | هشدار صریحِ فایل بدون رمزگذاری و داده‌ی حساس اضافه شد |
| درخواست مجوز اعلان در اولین بازشدن برنامه بود | درخواست بی‌زمینه و احتمال رد شدن بالا | فقط هنگام ورود کاربر به بخش هشدارها درخواست می‌شود |
| لغو coroutine در updater/Worker می‌توانست به خطای عادی تبدیل شود | کار لغوشده به‌صورت retry/error ادامه پیدا می‌کرد | `CancellationException` دوباره پرتاب می‌شود |

### نتیجه‌ی فروشگاه

نسخه‌ی فعلی برای Google Play هنوز **آماده‌ی ارسال نیست**. Target API در نسخه‌ی ۱٫۱۹
به ۳۶ ارتقا یافت، اما موانع باقی‌مانده شامل معماری سرویس زنده‌ی طولانی، کلید امضای عمومی، نبود
AAB انتشار، updater بیرون از Play و نبود Privacy Policy/Data safety آماده است. تحلیل و
نقشه‌ی اقدام کامل در [`STORE_READINESS_fa.md`](STORE_READINESS_fa.md) ثبت شده است.

## ۷) قابلیت‌های نسخه‌ی ۱٫۱۵

- تمام رابط فارسی Compose و Activity مستقل از زبان گوشی با جهت اجباری RTL اجرا می‌شود؛ ویجت‌های RemoteViews نیز RTL هستند.
- تاریخچه‌ی محلیِ حداکثر ۲۰۰ اعلان تحویل‌شده اضافه شد؛ پاک‌سازی دستی و ورود/خروج در بکاپ دارد.
- شرط `VOLUME_SPIKE` جهش درصدی حجم را از دو نمونه‌ی متوالی محاسبه و با cooldown کنترل می‌کند.
- صفحه‌ی «سلامت منابع» آخرین موفقیت/خطا، زمان پاسخ، میزبان و fallback فعال را نشان می‌دهد. endpoint شکست‌خورده ۵ دقیقه به انتهای صف می‌رود و بعد دوباره آزمایش می‌شود.
- جهش قیمت توضیح‌داده‌نشده‌ی ۴۰٪+ یک نمونه قرنطینه می‌شود؛ آخرین قیمت سالم باقی می‌ماند و نمونه‌ی دومِ هم‌محدوده تغییر را تأیید می‌کند.
- واچ‌لیست‌های نام‌دار سراسری اضافه شد و همراه بکاپ ذخیره/بازیابی می‌شود.
- نسخه به `1.15` و `versionCode=16` افزایش یافت.

## ۸) قابلیت‌های نسخه‌ی ۱٫۱۶

- آلارم پامپ به‌صورت opt-in به تنظیمات هر ویجت افزوده شد و از چرخه‌ی موجود سرویس/Worker استفاده می‌کند.
- اسکن CoinGecko مستقل از فاصله‌ی چندثانیه‌ای ویجت، حداقل ۱۵ دقیقه فاصله دارد؛ هر اسکن برای هر ویجت فقط یک‌بار ارزیابی می‌شود و cooldown اعلان بین ۳۰ دقیقه تا ۶ ساعت قابل انتخاب است.
- هر کوین یک پیشنهاد احتیاطیِ pure و قابل‌آزمون با سه خروجی «فقط زیر نظر بگیر»، «برای ورود عجله نکن» یا «از تعقیب قیمت دوری کن» دارد.
- دلیل پیشنهاد از سطح ریسک، شتاب یک‌ساعته، رشد ۲۴ساعته و نسبت حجم به ارزش بازار استخراج و هم در نتیجه‌ی اسکن و هم در اعلان فارسی RTL نمایش داده می‌شود؛ هیچ خروجی «خرید» وجود ندارد.
- انتخاب بهترین کاندید اعلان و شاخه‌های پیشنهاد با unit test پوشش داده شدند.
- نسخه به `1.16` و `versionCode=17` افزایش یافت.

### اعتبارسنجی نسخه‌ی ۱٫۱۶

Commit نهایی قابلیت‌ها `7b91e67f7e7dfcaa6ea49436dac944ae532b5268` در GitHub Actions با
`assembleDebug`، همه‌ی unit testها، `lintDebug` و بررسی امضای ثابت APK با موفقیت عبور کرد.
ران: [36231778773](https://github.com/sargoliwork101-ui/analysis-trades-/actions/runs/36231778773).

### اعتبارسنجی نسخه‌ی ۱٫۱۵

Commit نهایی قابلیت‌ها `668d3126609781fbc0065a26148f3a0c7664a2d7` در GitHub Actions با
`assembleDebug`، همه‌ی unit testها، `lintDebug` و بررسی امضای APK با موفقیت عبور کرد.
ران: [36230722907](https://github.com/sargoliwork101-ui/analysis-trades-/actions/runs/36230722907).

### اعتبارسنجی نهایی ممیزی دوم

Commit عملکردی `de89bc565ebe79ae02fa41de2ed6dc152b52aa03` در GitHub Actions با موفقیت
اعتبارسنجی شد: `assembleDebug`، همه‌ی unit testها، `lintDebug` و بررسی امضای APK
همگی سبز بودند. ران: [36223816914](https://github.com/sargoliwork101-ui/analysis-trades-/actions/runs/36223816914).

## ۹) قانون نسخه‌گذاری از نسخه‌ی ۱٫۱۷

قانون اجباری «هر تغییر، یک نسخه» در `AGENTS.md` ریشه‌ی مخزن و چک‌لیست
`ARCHITECTURE_fa.md` ثبت شد. از این نسخه حتی تغییر کوچک، رفع اشکال یا مستندات باید
`versionCode` و `versionName` تازه و ورودی تاریخچه‌ی نسخه‌ها داشته باشد؛ مجموعه‌تغییر
بدون نسخه‌ی تازه نباید commit یا push شود.

## ۱۰) نظر دوم هوش مصنوعی پامپ در نسخه‌ی ۱٫۱۸

- اتصال به مدل خاصی قفل نشده است: آدرس Chat Completions سازگار با OpenAI، نام مدل و API Key را کاربر تعیین می‌کند.
- اجرا فقط با اقدام مستقیم کاربر و برای همان کوین است؛ بنابراین هزینه و سهمیه‌ی API در اسکن‌های پس‌زمینه مصرف نمی‌شود.
- داده‌های قیمت، حجم، ارزش بازار، ریسک و پیشنهاد پایه برای بررسی فرستاده می‌شوند. جست‌وجوی وب خود ارائه‌دهنده درخواست می‌شود و خبرهای برگشتی فقط با URL مبتنی بر HTTP(S) نمایش داده می‌شوند.
- AI «نظر دوم» است و پیشنهاد deterministic برنامه را بازنویسی نمی‌کند. پاسخ به سه پیشنهاد احتیاطی محدود می‌شود و توصیه‌ی مستقیم خرید/سود تضمینی در parser رد می‌شود.
- API Key در SharedPreferences خصوصی جدا از `WidgetConfig` نگه‌داری می‌شود، وارد بکاپ دستی نیست و با `allowBackup=false` وارد بکاپ خودکار اندروید نیز نمی‌شود.
- parser پاسخ، محدودسازی لینک‌ها، رد توصیه‌ی مستقیم و ساخت آدرس endpoint با unit test پوشش داده شدند.
- نسخه به `1.18` و `versionCode=19` افزایش یافت.

## ۱۱) ممیزی امنیت و پایداری نسخه‌ی ۱٫۱۹

### اصلاحات اعمال‌شده

- `compileSdk` و `targetSdk` به API 36، AGP به 8.9.1، Gradle به 8.11.1 و WorkManager به 2.11.2 ارتقا یافتند؛ CI نصب صریح Android 16 SDK را انجام می‌دهد.
- `LiveUpdateService.onTimeout` اضافه شد تا پایان سهمیه‌ی `dataSync` در Android 15+ به crash منجر نشود.
- API Key هوش مصنوعی از plaintext به AES-GCM با کلید Android Keystore مهاجرت کرد؛ plaintext قدیمی پس از خواندن حذف می‌شود و کلید روی HTTP ارسال نمی‌شود.
- redirect خودکار OkHttp حذف شد: فقط GET تا سه مرحله دنبال می‌شود، downgrade از HTTPS به HTTP رد می‌شود، POST دارای بدنه redirect نمی‌شود و credential در تغییر origin حذف می‌شود.
- هدرهای Authorization، Cookie، API key، token، password و secret برای منابع HTTP حذف می‌شوند و credential در user-info/query HTTP رد می‌شود؛ cleartext فقط برای داده‌ی عمومیِ انتخاب‌شده‌ی کاربر باقی ماند.
- پاک‌سازی متمرکز URL/credential به خطاهای شبکه، سلامت منبع، خطای ویجت و اسکن پامپ اعمال شد؛ حالت `Authorization: Bearer …` و JSON key/value نیز پوشش داده شد.
- وضعیت crossing و cooldown هشدار قیمت با شناسه‌ی ویجت namespace شد؛ rule کپی‌شده در دو ویجت دیگر اعلان‌های یکدیگر را خاموش یا جایگزین نمی‌کند.
- عقب‌کشیدن ساعت گوشی دیگر کش و cooldown قیمت/پامپ را برای مدت نامحدود قفل نمی‌کند.
- endpoint هوش مصنوعی query، fragment و user-info را رد می‌کند؛ خبر فقط با HTTPS پذیرفته و میزبان واقعی لینک مستقل از ادعای مدل نمایش داده می‌شود.
- خروجی AI به verdictهای مجاز محدود و control/bidi override حذف شد؛ توصیه‌ی مستقیم خرید و سود تضمینی سخت‌گیرانه‌تر رد می‌شود.
- متن CoinGecko، نمادهای سفارشی TSE و ورودی جست‌وجو محدود/پاک‌سازی شدند؛ `Throwable` دیگر در مسیر TSE به‌طور عمومی بلعیده نمی‌شود و cancellation در refresh/service/backup حفظ می‌شود.
- `BootReceiver` غیرexported شد و Activity فقط شناسه‌ی واقعی یکی از ویجت‌های همین برنامه را می‌پذیرد.
- CI قانون «هر تغییر، یک نسخه» را با مقایسه‌ی `versionCode` و `versionName` با commit والد enforce می‌کند.
- تست‌های خالص برای redaction، هدر HTTP، rollback ساعت، اعتبارسنجی endpoint، متن remote و سخت‌گیری پاسخ AI افزوده شد.
- نسخه به `1.19` و `versionCode=20` افزایش یافت.

### ریسک‌های باقی‌مانده

- کلید امضای APK مستقل/GitHub همچنان عمومی است و برای انتشار فروشگاهی باید با upload key خصوصی و secretهای CI جایگزین شود.
- سرویس زنده‌ی چندثانیه‌ای با وجود `onTimeout` برای سیاست Google Play باید جلسه‌ای/محدود شود یا پایش لحظه‌ای به backend و FCM منتقل شود.
- build فروشگاهیِ AAB، flavor بدون updater خارجی، Privacy Policy عمومی، Data Safety و declarationهای مالی/FGS هنوز آماده نشده‌اند.
- HTTP دلخواه بدون credential هنوز ذاتاً قابل دست‌کاری است؛ UI هشدار می‌دهد و HTTPS توصیه می‌شود.

### اعتبارسنجی نسخه‌ی ۱٫۱۹

Commit عملکردی `ff5e478c607542709a5219b1df087882ae908151` در GitHub Actions با نصب SDK 36،
کنترل افزایش نسخه، `assembleDebug`، همه‌ی unit testها، `lintDebug` و بررسی امضای ثابت APK
با موفقیت عبور کرد. ران: [36238080502](https://github.com/sargoliwork101-ui/analysis-trades-/actions/runs/36238080502).
