# راهنمای دریافت فایل نصبی (APK) از طریق گیت‌هاب (GitHub Actions)

پروژه به صورت کامل به سیستم **بیلد خودکار (CI/CD)** مجهز شده است. با فرستادن (Push) این پوشه به یک ریپازیتوری در گیت‌هاب، سرورهای گیت‌هاب به صورت خودکار پروژه را کامپایل کرده و فایل نصبی **APK آماده‌ی نصب روی گوشی** را در اختیارتان قرار می‌دهند!

---

## 🚀 گام ۱: ساخت ریپازیتوری در گیت‌هاب

۱. وارد حساب کاربری خود در [github.com](https://github.com) شوید.
۲. از بالا سمت راست روی علامت **+** زده و **New repository** را انتخاب کنید.
۳. نام ریپازیتوری را مثلاً `pulse-widget` بگذارید.
۴. حالت آن را می‌توانید **Public** (عمومی) یا **Private** (خصوصی) انتخاب کنید (در هر دو حالت کار می‌کند).
۵. گزینه‌های *Add a README* یا *.gitignore* را **تیک نزنید** (چون در این پروژه وجود دارند).
۶. دکمه‌ی سبز **Create repository** را بزنید.

---

## 💻 گام ۲: فرستادن پروژه به گیت‌هاب (Push)

ترمینال یا CMD را در پوشه‌ی پروژه باز کنید و دستورات زیر را وارد کنید:
*(آدرس زیر را با آدرس ریپازیتوری خود در گیت‌هاب جایگزین کنید)*

```bash
# اتصال به ریپازیتوری گیت‌هاب شما
git remote add origin https://github.com/نام_کاربری_شما/pulse-widget.git

# تغییر شاخه به main (در صورت نیاز)
git branch -M main

# فرستادن کدها به گیت‌هاب
git push -u origin main
```

> **نکته:** اگر از برنامه گرافیکی **GitHub Desktop** استفاده می‌کنید، کافیست گزینه‌ی `Add Existing Repository` را بزنید و این پوشه را انتخاب کرده و Publish کنید.

---

## ⚙️ گام ۳: بیلد خودکار در سرور گیت‌هاب

به محض انجام `push`، سرورهای ابری گیت‌هاب شروع به کار می‌کنند:
۱. در صفحه‌ی ریپازیتوری خود در گیت‌هاب، به تب **Actions** (در نوار بالای صفحه) بروید.
۲. یک عملیات با عنوان **Build Android APK** در حال اجرا می‌بینید (آیکون زرد چشمک‌زن).
۳. بیلد بین **۲ تا ۳ دقیقه** طول می‌کشد تا تمام شود و آیکون آن **تیک سبز ✓** بخورد.

> **اجرای دستی (اختیاری):**
> هر زمان خواستید مجدداً بدون تغییر کد فایل APK جدیدی ساخته شود:
> به تب **Actions** بروید ➔ روی **Build Android APK** کلیک کنید ➔ دکمه‌ی **Run workflow** را بزنید.

---

## 📱 گام ۴: دانلود فایل APK و نصب روی گوشی

۱. روی بیلد انجام‌شده (تیک سبز) در تب **Actions** کلیک کنید.
۲. در پایین صفحه، بخشی به نام **Artifacts** وجود دارد.
۳. روی فایل **`PulseMarket-Android-APK`** کلیک کنید تا دانلود شود.
۴. فایل دانلود شده را باز (یا Extract) کنید؛ فایل **`PulseMarket-v1.0.apk`** داخل آن قرار دارد.
۵. این فایل را به گوشی منتقل کنید (با تلگرام، بلوتوث، کابل یا مستقیماً با مرورگر گوشی دانلود کنید).
۶. روی فایل `.apk` ضربه بزنید و **Install** را بزنید:
   - در صورت پیام امنیتی «نصب برنامه‌های ناشناس»، گزینه‌ی Allow from this source را تأیید کنید.

---

## 📈 نحوه کار با نمادهای بورس تهران در اپلیکیشن

۱. برنامه **«نبض بازار»** را روی گوشی باز کنید.
۲. منبع را روی **«بورس تهران — TSETMC»** بگذارید.
۳. دکمه‌ی **🔍 جستجو در بورس تهران یا افزودن با لینک TSETMC** را بزنید:
   - **روش اول (جستجو با نام):** نام هر سهمی مثل `اهرم`، `عیار`، `طلا`، `خودرو`، `خساپا`، `شتران`، `فولاد`، `دی` و... را تایپ کنید و جستجو را بزنید.
   - **روش دوم (با لینک TSETMC):** وارد سایت [tsetmc.com](https://tsetmc.com) شوید، صفحه‌ی سهم مورد نظرتان را باز کنید و آدرس URL آن را کپی کنید (مثلاً `https://tsetmc.com/instInfo/46348633615832441` یا ISIN یا نسخه قدیم). سپس دکمه‌ی **چسباندن لینک** را بزنید.
   - **روش سوم (انتخاب سریع):** روی چیپ‌های آماده‌ی پرطرفدار (صندوق‌های طلا، اهرمی‌ها، شاخص‌سازها) کلیک کنید.
۴. روی دکمه‌ی **+ افزودن** کنار نماد دلخواه بزنید.
۵. در صفحه اصلی برنامه، دکمه‌ی **«ذخیره و به‌روزرسانی»** را بزنید.
۶. ویجت را روی صفحه اصلی گوشی (Home Screen) بچینید؛ قیمت لحظه‌ای، رنگ درصد سود/زیان روزانه و نمودار برای سهم شما نمایش می‌یابد!
